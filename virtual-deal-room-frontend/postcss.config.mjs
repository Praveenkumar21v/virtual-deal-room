const config = {
  plugins: {
    '@tailwindcss/postcss': {}, // Use the dedicated PostCSS plugin
    autoprefixer: {}, // Optional but recommended for cross-browser compatibility
  },
};

export default config;